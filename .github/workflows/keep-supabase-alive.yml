name: Keep Supabase Alive

on:
  schedule:
    # Run every day at midnight (UTC)
    - cron: '0 0 * * *'
  workflow_dispatch: # Allow manual trigger

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase URL
        run: |
          curl -I https://lwdgrjxgwtcqfctqbbbu.supabase.co
